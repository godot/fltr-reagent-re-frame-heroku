(ns oxford-web-app.server
  ;;   (:require
  ;; ;;   [oxford-web-app.handler :refer [app]]
  ;; ;;   [ring/ring-jetty-adapter :refer [run-jetty]])
  ;;   (:gen-class)
  )

;; (defn -main [& [port]]
;;   (let [port (Integer. (or port (env :port) 5000))]
;;     (run-jetty app {:port port :join? false})))
